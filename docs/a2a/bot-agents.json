{
  "version": "1.0.0",
  "description": "Grok Bot seats on the local A2A bus (not grok agent serve / acp_inject).",
  "seats": {
    "orchestrator": {
      "kind": "grok-bot",
      "agentId": "REPLACE_WITH_YOUR_GROK_BOT_AGENT_ID",
      "inbox": ".a2a-state/orchestrator",
      "note": "Default Bot orchestrator seat. Put Bot seats in registry skipSeats. bot-bridge is opt-in (GCS_BOT_BRIDGE=1) and wakes via bot-wake artifacts. Not an ACP inject target. Default: Bot seats standby."
    }
  }
}
