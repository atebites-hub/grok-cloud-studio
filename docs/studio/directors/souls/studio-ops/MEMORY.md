# Memory — studio-ops

Bus and daemon notes live here across turns. First-class seat; not only an ops alias.

Board host kit: `scripts/studio/taskboard/maintainer.sh` (start/stop/status/health/docs) and `health-taskboard.sh`. Pin stays tcarac/taskboard v0.6.0. Do not reconnect Agent Kanban.
