# LEGACY studio dashboard

This local HTML dashboard is **LEGACY**.

Studio mission control is Agent Kanban. See [`docs/studio/AGENT_KANBAN.md`](../../../docs/studio/AGENT_KANBAN.md) for install, `GCS_AGENT_KANBAN_*` aliases, sync-only fleet bridging, and why `ak start` is not the bus default.
