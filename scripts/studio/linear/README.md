# Living Sky Linear stamp (mind CLI)

Grok Build minds stamp `LIV-*` themselves after a TASK completes.

```bash
python3 scripts/studio/linear/liv_stamp.py after-task \
  --issue LIV-82 --task "$A2A_TASK_ID" --seat floor \
  --evidence "CLOUD_LAUNCH_OK id=bc-…"
```

Living Sky only (`linear.app/livingsky`, team `LIV`). NEVER Black Swan
Money. Donald does not DIY Linear.

See `docs/studio/LINEAR.md` and `docs/studio/bdd/liv_stamp_after_task.feature`.
