# Grok Cloud Studio — placeholders only. Copy to .env and fill locally.
# NEVER commit real values.

CURSOR_API_KEY=
# Or: ~/.config/cursor/agent.env containing CURSOR_API_KEY=...

GCS_CLOUD_REPO=https://github.com/ORG/REPO
GCS_CLOUD_REF=main

CURSOR_CLOUD_MODEL=grok-4.6
CURSOR_CLOUD_EFFORT=xhigh

GCS_A2A_HOST=127.0.0.1
GCS_A2A_PORT=8732
GCS_A2A_HUB=http://127.0.0.1:8732

GCS_WEBHOOK_SECRET=
GCS_WEBHOOK_HOST=127.0.0.1
GCS_WEBHOOK_PORT=8788
