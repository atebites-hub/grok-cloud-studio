[pytest]
testpaths = tests
pythonpath = scripts
# Quiet is the ship-gate CLI flag `.venv/bin/pytest -q` (AGENTS.md).
# Do not set addopts=-q: pytest 8+ treats -q twice as extra-quiet and hides
# the passed count (LIV-74).
