Feature: Grok Build mind Extra High spawn execs launch-cloud-extra-high.sh
  Unique remaining vs origin/main. Grok Build mind Extra High spawn must
  exec scripts/launch-cloud-extra-high.sh (or cloud_launch). Never Bot CloudAgent.
  Never grok --resume for Cloud create. Do not vendor Hermes.
  Do not merge harvest mailbox PRs #26 and #28. Model grok-4.6 xhigh.

  Demonstrate, don't theatre.

  Scenario: Mailbox wrap names Extra High spawn as launch-cloud-extra-high.sh
    Given a Grok Build mind mailbox turn
    When wrap_mind_mail prepends spawn law
    Then the runner prompt names cloud_launch or scripts/launch-cloud-extra-high.sh
    And it forbids grok --resume for Cloud create
    And it forbids Bot CloudAgent as the grunt
    And Extra High is grok-4.6 xhigh

  Scenario: cloud_launch plugin execs the Extra High launcher
    Given studio-mind cloud_launch
    When the plugin builds Cloud create argv
    Then argv0 is scripts/launch-cloud-extra-high.sh
    And argv is not grok --resume
    And argv is not Bot CloudAgent

  Scenario: Mind spawn PATH wrapper execs the Extra High launcher
    Given a Grok Build mind GROK_HOME/bin/cloud_launch wrapper
    When the mind loop installs remaining spawn PATH wrappers
    Then the wrapper execs scripts/launch-cloud-extra-high.sh
    And it does not exec grok --resume
    And the launcher is executable in git
